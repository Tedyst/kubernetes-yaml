apiVersion: apps/v1
kind: Deployment
metadata:
  name: nextcloud
  namespace: tedy
  labels:
    app: nextcloud
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nextcloud
  template:
    metadata:
      labels:
        app: nextcloud
    spec:
      containers:
        - name: nextcloud
          image: nextcloud:18.0.1
          resources:
            limits:
              cpu: "100m"
              memory: "300M"
            requests:
              cpu: "100m"
              memory: "300M"
          volumeMounts:
            - name: data
              mountPath: /var/www/html/
      volumes:
        - name: data
          hostPath:
            path: /media/hddext4/nextcloud
